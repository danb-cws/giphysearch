{
  "name": "Giphysearch",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "debounce": "^1.1.0",
    "normalize.css": "^8.0.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "devDependencies": {
    "enzyme": "^3.4.1",
    "enzyme-adapter-react-16": "^1.2.0"
  },
  "browserslist": [
    "last 2 versions",
    "not IE > 0"
  ],
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "firebase deploy --project giphysearch-danb",
    "release": "npm run build && npm run deploy"
  }
}
